export const functions = {
  argsAsArray: (fn, arr) => {

  },

  functionFunction: (str) => {

  },

  makeClosures: (arr, fn) => {

  },

  partial: (fn, str1, str2) => {

  },

  useArguments: (...args) => {

  },

  callIt: (fn, ...args) => {

  },

  partialUsingArguments: (fn, ...args) => {

  },

  curry: (fn) => {
  }
};
